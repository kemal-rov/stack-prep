/*
How do you handle waiting for an element to appear 
in Playwright before interacting with it?
*/

// Cypress automatically retries commands, 
// so you can use should('be.visible') to wait for elements.

// placeholder